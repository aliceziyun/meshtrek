apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: geo-l4-policy
  namespace: hotel
spec:
  podSelector:
    matchLabels:
      app: hotel
      io.kompose.service: geo

  policyTypes:
  - Egress

  egress:
  - to:
    - podSelector:
        matchLabels:
          io.kompose.service: mongodb-geo
    ports:
    - protocol: TCP
      port: 27017

  - to:
    - podSelector:
        matchLabels:
          io.kompose.service: memcached-geo
    ports:
    - protocol: TCP
      port: 11211