apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: user-l4-policy
  namespace: hotel
spec:
  podSelector:
    matchLabels:
      app: hotel
      io.kompose.service: user

  policyTypes:
  - Egress

  egress:
  - to:
    - podSelector:
        matchLabels:
          io.kompose.service: mongodb-user
    ports:
    - protocol: TCP
      port: 27017

  - to:
    - podSelector:
        matchLabels:
          io.kompose.service: memcached-user
    ports:
    - protocol: TCP
      port: 11211